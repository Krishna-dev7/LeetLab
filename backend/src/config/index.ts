const appConfig = {
  DEFAULT_SERVER_PORT: 3000,
  DEFAULT_SERVER_ORIGIN: "http://localhost:3000"
}


const authConfig = {
  JWT_SECRET: "krishnasleetlab",
}

export default appConfig;
export {
  authConfig
};